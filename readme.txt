What I have done so far

1. Used my personal AWS account to create AWS instance it is of () type
2. This instance created manually, gave name "dev-machine"
3. Connected to ssh web console by clikcing on Connect button
4. Installed below tools
    a. git bash
          * Configure git ssh to connect to repository
          * Perform various git tasks like:
              -> git clone
              -> git pull/commit/push
              -> git config changes
    b. terraform
    c. ngnix server

This repository is created to build AWS infrastructure using terraform
1. lets begin with creating ec2 instance with ngnix web server
